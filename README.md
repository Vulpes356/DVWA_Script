# DVWA - Vulpes356 coding

Just my own script when working DVWA
🚩Not using these for attacking real system

-> Python version - 3.11+

Request: having `requests` library in Python. If don't, using this command to install
```python
pip install requests
```
